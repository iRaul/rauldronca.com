import styled from 'styled-components';

export const ProfileImage = styled.img`
  width: 100%;
  object-fit: cover;
  border-radius: 4px;
  display: block;
  mix-blend-mode: screen;
  margin-bottom: 60px;
  margin-top: -30px;
`;
